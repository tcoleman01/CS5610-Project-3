import React, { useState } from "react";
import { Modal, Button, Form, Row, Col } from "react-bootstrap";

export default function TripFormModal({ show, onHide, onSave }) {
    return <p>test</p>;
    // Placeholder for TripFormModal component
}